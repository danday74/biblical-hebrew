{
  "name": "biblical-hebrew",
  "version": "1.0.0",
  "description": "A tool for learning Biblical Hebrew vocabulary",
  "author": "danday74",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start": "pm2 start",
    "stop": "pm2 delete all",
    "test": "echo \"Error: no test specified\" && exit 1",
    "preinstall": "git submodule foreach \"npm i\""
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-sort-requires": "^2.1.0"
  }
}
